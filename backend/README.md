# AI 情绪日记系统

一个温馨的 AI 日记与成长报告系统，帮助用户记录日常、生成总结与成长分析。

## 功能特点

- 📖 **日记生成**：与 AI 对话生成温馨日记
- 📊 **周报/月报**：自动生成成长报告
- 📈 **成长记录**：情绪曲线、主题雷达、活动热力图
- 💬 **情绪树洞**：情感陪伴对话

## 技术栈

- **前端**：React + Vite + Tailwind CSS
- **后端**：Node.js + Express
- **数据库**：SQLite
- **AI**：DeepSeek API
- **图表**：Recharts

## 安装步骤

### 1. 安装后端依赖

```bash
cd backend
npm install
```

### 2. 安装前端依赖

```bash
cd frontend
npm install
```

## 运行项目

### 启动后端服务器

```bash
cd backend
npm start
```

后端将在 `http://localhost:3001` 运行

### 启动前端开发服务器

```bash
cd frontend
npm run dev
```

前端将在 `http://localhost:3000` 运行

## 使用说明

1. **日记生成**：
   - 点击书本打开对话界面
   - 输入今天想记录的内容
   - AI 会生成一篇温馨的日记

2. **周报/月报**：
   - 点击"查看记录"查看日记
   - 点击"汇总成周报/月报"生成报告

3. **成长记录**：
   - 查看情绪曲线、主题雷达和活动热力图
   - 了解自己的情绪趋势和活动规律

4. **情绪树洞**：
   - 与 AI 自由对话
   - 可选择是否纳入成长分析

5. **清除数据**：
   - 在侧边栏点击"清除所有数据"
   - 确认后删除所有记录

## 项目结构

```
情绪日记网站/
├── backend/          # 后端服务
│   ├── server.js    # 服务器主文件
│   └── diary.db     # SQLite 数据库（自动生成）
├── frontend/         # 前端应用
│   ├── src/
│   │   ├── components/  # 组件
│   │   ├── pages/       # 页面
│   │   └── App.jsx      # 主应用
│   └── ...
└── README.md
```

## 注意事项

- 确保后端服务已启动后再访问前端
- API 密钥已配置在 `backend/server.js` 中
- 数据库文件会在首次运行时自动创建
- 清除数据操作不可恢复，请谨慎使用





